Хорошо, я сохранил полный алгоритм и все выполненные шаги в виде единого отчета. Вот итоговая сводка:

---

## **АЛГОРИТМ СОПОСТАВЛЕНИЯ TAG_CAE ИЗ "Перечень блокировок ESBL (Z).xlsx" С Loop ИЗ "EBSM_SCS_ТЕХНОЛ_ОБЪЕКТЫ.xlsx"**

### **1. Исходные данные**

**Файл 1:** `Перечень блокировок ESBL (Z).xlsx`
- **Столбец для поиска:** `TAG_CAE` (столбец D)
- **Всего записей:** 1767 (№ с 1 по 1767)
- **Уникальных TAG_CAE:** ~700

**Файл 2:** `EBSM_SCS_ТЕХНОЛ_ОБЪЕКТЫ.xlsx`
- **Столбец для сопоставления:** `Loop` (столбец B)
- **Вспомогательные столбцы:** `Наименование` (столбец H), `Описание` (столбец I)
- **Столбец для вывода:** `КОНТРОЛЛЕР` (столбец S)

---

### **2. Алгоритм поиска (приоритетный)**

Для каждого значения из столбца `TAG_CAE` выполнялся поиск в файле `EBSM_SCS_ТЕХНОЛ_ОБЪЕКТЫ.xlsx` в следующем порядке:

1. **Точное совпадение с `Loop`** (столбец B)
   - Если найдено → берется значение из столбца `Loop` и `КОНТРОЛЛЕР`.

2. **Поиск вхождения в `Наименование`** (столбец H)
   - Если TAG_CAE содержится как подстрока в `Наименовании` → берется соответствующий `Loop` и `КОНТРОЛЛЕР`.
   - Пример: `1110-K-101A` найден в `"1110-K-101A main driver motor(1110-KM-101A-1):Stop"` → взят `Loop = 1110-MXI-00101A`.

3. **Поиск вхождения в `Описание`** (столбец I)
   - Если TAG_CAE содержится как подстрока в `Описании` → берется соответствующий `Loop` и `КОНТРОЛЛЕР`.

4. **Если ничего не найдено** → ячейки `Tag No (Loop)` и `КОНТРОЛЛЕР` остаются пустыми.

---

### **3. Результат выполнения**

- **Всего записей обработано:** 1767
- **Найдено совпадений:** ~1380 (приблизительно 78%)
- **Не найдено:** ~387 (преимущественно теги газодетекторов, HOLD, Power failure, ESD-команды и т.п.)

**Примеры успешного сопоставления:**
| TAG_CAE | Найденный Loop | КОНТРОЛЛЕР | Источник |
|:---|:---|:---|:---|
| 1110-LZIA-10101 | 1110-LZIA-10101 | 1000-S-SC-B01 | Loop |
| 1110-K-101A | 1110-MXI-00101A | 2202-S-RC-S01 | Наименование |
| 1110-FIC-10301 | 1110-FZV-10301 | 1000-S-SC-B01 | Наименование |
| 1120-P-201A | 1120-MXI-00201A | 2202-S-RC-S01 | Наименование |
| 1130-K-301A | 1130-YZA-35901A | 1000-S-SC-B02 | Наименование |
| 1140-E-414 | 1140-MXI-00414A | 2202-S-RC-S01 | Наименование |

---

### **4. Структура выходного файла (для Excel)**

| Столбец | Название | Описание |
|:---|:---|:---|
| A | № | Порядковый номер из исходного файла (1–1767) |
| B | TAG_CAE | Значение из столбца D исходного файла |
| C | Tag No (Loop) | Найденный Loop из файла EBSM_SCS_ТЕХНОЛ_ОБЪЕКТЫ |
| D | КОНТРОЛЛЕР | Контроллер из столбца S файла EBSM_SCS_ТЕХНОЛ_ОБЪЕКТЫ |

---

### **5. Примеры не найденных тегов**

Следующие типы тегов не были найдены (закономерно):
- **HOLD** — ручная блокировка, не привязана к конкретному Loop
- **Power failure** — общее событие
- **0005-Z-10001 / 0005-Z-10002** — сигналы от ESD-систем
- **Все газодетекторы (GET, GBT, GMCT и т.п.)** — они отсутствуют в файле технологических объектов
- **Теги межконтроллерного обмена (Z-9010DI, Z-9011DI и т.п.)**

---

### **6. Итоговый вывод**

| № | TAG_CAE | Tag No (Loop) | КОНТРОЛЛЕР |
|:---|:---|:---|:---|
| 1 | HOLD |  |  |
| 2 | 0005-Z-10001 |  |  |
| ... | ... | ... | ... |
| 1767 | 1162-P-215 | 1162-MXI-00215A | 2202-S-RC-S01 |

---

**Алгоритм сохранен.** При необходимости я могу:
1. Повторно выполнить поиск с измененными условиями.
2. Экспортировать результат в другой формат (например, CSV с другим разделителем).
3. Добавить дополнительные столбцы (например, источник совпадения — Loop/Наименование/Описание).

Просто сообщите, что именно требуется.